# coding=utf-8

# Add your secrets to this list and run manage.py to set their values.
# Use them in settings like this:
# from secrets import secrets
# SECRET_KEY = secrets.SECRET_KEY

SECRET_KEYS = [
    "DJANGO_SECRET_KEY",
    "DB_NAME",
    "DB_USER",
    "DB_PASSWORD",
]
